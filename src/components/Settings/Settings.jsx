import React from "react";
import s from './Settings.module.css';

const Settings = (props) => {
    return(
        <div className={s.settings}>
            <div className={s.settings__menu}>Settings</div>
        </div>
    )
}

export default Settings;